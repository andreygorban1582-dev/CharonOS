# This directory stores CharonOS persistent memory (JSON files).
# The files here are committed back to GitHub automatically by the agent
# so that context is preserved across Codespace restarts.
#
# conversations.json — per-user message history
# facts.json         — global key/value facts
